# Generated by rust2rpm 13
%bcond_without check
%global debug_package %{nil}

%global crate copypasta

Name:           rust-%{crate}
Version:        0.7.0
Release:        1%{?dist}
Summary:        Cross-platform clipboard library with Wayland support

# Upstream license specification: MIT / Apache-2.0
License:        MIT or ASL 2.0
URL:            https://crates.io/crates/copypasta
Source:         %{crates_source}
# Remove other platforms (e.g. macOS)
Patch0:         copypasta-rm-other-platforms.diff

ExclusiveArch:  %{rust_arches}
%if %{__cargo_skip_build}
BuildArch:      noarch
%endif

BuildRequires:  rust-packaging

%global _description %{expand:
Cross-platform library for getting and setting the contents of the OS-level
clipboard, with support for the Wayland clipboard.}

%description %{_description}

%package        devel
Summary:        %{summary}
BuildArch:      noarch

%description    devel %{_description}

This package contains library source intended for building other packages
which use "%{crate}" crate.

%files          devel
%license LICENSE.mit LICENSE.apache2
%doc README.md
%{cargo_registry}/%{crate}-%{version_no_tilde}/

%package     -n %{name}+default-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+default-devel %{_description}

This package contains library source intended for building other packages
which use "default" feature of "%{crate}" crate.

%files       -n %{name}+default-devel
%ghost %{cargo_registry}/%{crate}-%{version_no_tilde}/Cargo.toml

%package     -n %{name}+smithay-clipboard-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+smithay-clipboard-devel %{_description}

This package contains library source intended for building other packages
which use "smithay-clipboard" feature of "%{crate}" crate.

%files       -n %{name}+smithay-clipboard-devel
%ghost %{cargo_registry}/%{crate}-%{version_no_tilde}/Cargo.toml

%package     -n %{name}+wayland-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+wayland-devel %{_description}

This package contains library source intended for building other packages
which use "wayland" feature of "%{crate}" crate.

%files       -n %{name}+wayland-devel
%ghost %{cargo_registry}/%{crate}-%{version_no_tilde}/Cargo.toml

%package     -n %{name}+x11-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+x11-devel %{_description}

This package contains library source intended for building other packages
which use "x11" feature of "%{crate}" crate.

%files       -n %{name}+x11-devel
%ghost %{cargo_registry}/%{crate}-%{version_no_tilde}/Cargo.toml

%package     -n %{name}+x11-clipboard-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+x11-clipboard-devel %{_description}

This package contains library source intended for building other packages
which use "x11-clipboard" feature of "%{crate}" crate.

%files       -n %{name}+x11-clipboard-devel
%ghost %{cargo_registry}/%{crate}-%{version_no_tilde}/Cargo.toml

%prep
%autosetup -n %{crate}-%{version_no_tilde} -p1
%cargo_prep

%generate_buildrequires
%cargo_generate_buildrequires

%build
%cargo_build

%install
%cargo_install

%if %{with check}
%check
%cargo_test
%endif

%changelog
* Fri May 22 12:14:07 PDT 2020 Michel Alexandre Salim <salimma@fedoraproject.org> - 0.7.0-1
- Initial package
