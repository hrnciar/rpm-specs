# Generated by rust2rpm 13
# Disable the check by default as it depend on rust-netlink-packet-route
# which cause loop dependency
%bcond_with check
%global debug_package %{nil}

%global crate netlink-packet-core

Name:           rust-%{crate}
Version:        0.2.1
Release:        1%{?dist}
Summary:        Netlink packet types

# Upstream license specification: MIT
# Pull request https://github.com/little-dude/netlink/pull/96 will
# include LICENSE file in crate
License:        MIT
URL:            https://crates.io/crates/netlink-packet-core
Source:         %{crates_source}

ExclusiveArch:  %{rust_arches}
%if %{__cargo_skip_build}
BuildArch:      noarch
%endif

BuildRequires:  rust-packaging

%global _description %{expand:
Netlink packet types.}

%description %{_description}

%package        devel
Summary:        %{summary}
BuildArch:      noarch

%description    devel %{_description}

This package contains library source intended for building other packages
which use "%{crate}" crate.

%files          devel
%{cargo_registry}/%{crate}-%{version_no_tilde}/

%package     -n %{name}+default-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+default-devel %{_description}

This package contains library source intended for building other packages
which use "default" feature of "%{crate}" crate.

%files       -n %{name}+default-devel
%ghost %{cargo_registry}/%{crate}-%{version_no_tilde}/Cargo.toml

%prep
%autosetup -n %{crate}-%{version_no_tilde} -p1
%cargo_prep

%generate_buildrequires
%cargo_generate_buildrequires

%build
%cargo_build

%install
%cargo_install

%if %{with check}
%check
%cargo_test
%endif

%changelog
* Fri Oct 09 2020 Gris Ge <fge@redhat.com> - 0.2.1-1
- Upgrade to 0.2.1

* Tue Jul 07 16:40:56 CST 2020 Gris Ge <cnfourt@gmail.com> - 0.2.0-1
- Initial package
