# Generated by go2rpm 1
%bcond_without check

# https://github.com/alexflint/go-scalar
%global goipath         github.com/alexflint/go-scalar
Version:                1.0.0

%gometa

%global common_description %{expand:
Scalar is a library for parsing strings into arbitrary scalars (integers,
floats, strings, booleans, etc). It is helpful for tasks such as parsing
strings passed as environment variables or command line arguments.}

%global golicenses      LICENSE
%global godocs          README.md

Name:           %{goname}
Release:        1%{?dist}
Summary:        Parsing library for string

License:        BSD
URL:            %{gourl}
Source0:        %{gosource}

%if %{with check}
# Tests
BuildRequires:  golang(github.com/stretchr/testify/assert)
BuildRequires:  golang(github.com/stretchr/testify/require)
%endif

%description
%{common_description}

%gopkg

%prep
%goprep

%install
%gopkginstall

%if %{with check}
%check
%gocheck
%endif

%gopkgfiles

%changelog
* Tue Apr 21 2020 Fabian Affolter <mail@fabian-affolter.ch> - 1.0.0-1
- Initial package

