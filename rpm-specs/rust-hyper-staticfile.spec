# Generated by rust2rpm 13
%bcond_without check
%global debug_package %{nil}

%global crate hyper-staticfile

Name:           rust-%{crate}
Version:        0.5.3
Release:        1%{?dist}
Summary:        Static file serving for Hyper 0.13

# Upstream license specification: MIT
License:        MIT
URL:            https://crates.io/crates/hyper-staticfile
Source:         %{crates_source}
# Initial patched metadata
# * No windows
Patch0:         hyper-staticfile-fix-metadata.diff

ExclusiveArch:  %{rust_arches}
%if %{__cargo_skip_build}
BuildArch:      noarch
%endif

BuildRequires:  rust-packaging

%global _description %{expand:
Static file serving for Hyper 0.13.}

%description %{_description}

%package        devel
Summary:        %{summary}
BuildArch:      noarch

%description    devel %{_description}

This package contains library source intended for building other packages
which use "%{crate}" crate.

%files          devel
%license LICENSE
%doc README.md
%{cargo_registry}/%{crate}-%{version_no_tilde}/

%package     -n %{name}+default-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+default-devel %{_description}

This package contains library source intended for building other packages
which use "default" feature of "%{crate}" crate.

%files       -n %{name}+default-devel
%ghost %{cargo_registry}/%{crate}-%{version_no_tilde}/Cargo.toml

%prep
%autosetup -n %{crate}-%{version_no_tilde} -p1
%cargo_prep

%generate_buildrequires
%cargo_generate_buildrequires

%build
%cargo_build

%install
%cargo_install

%if %{with check}
%check
%cargo_test
%endif

%changelog
* Tue May 26 2020 Josh Stone <jistone@redhat.com> - 0.5.3-1
- Update to 0.5.3

* Wed Apr 29 2020 Josh Stone <jistone@redhat.com> - 0.5.2-1
- Update to 0.5.2

* Mon Feb 17 10:38:59 CET 2020 Igor Raits <ignatenkobrain@fedoraproject.org> - 0.5.1-1
- Initial package
