# Generated by rust2rpm 13
%bcond_with check
%global debug_package %{nil}

%global crate netlink-packet-utils

Name:           rust-%{crate}
Version:        0.3.0
Release:        1%{?dist}
Summary:        Macros and helpers for parsing netlink messages

# Upstream license specification: MIT
# Pull request https://github.com/little-dude/netlink/pull/96 will
# include LICENSE file in crate
License:        MIT
URL:            https://crates.io/crates/netlink-packet-utils
Source:         %{crates_source}

ExclusiveArch:  %{rust_arches}
%if %{__cargo_skip_build}
BuildArch:      noarch
%endif

BuildRequires:  rust-packaging

%global _description %{expand:
Macros and helpers for parsing netlink messages.}

%description %{_description}

%package        devel
Summary:        %{summary}
BuildArch:      noarch

%description    devel %{_description}

This package contains library source intended for building other packages
which use "%{crate}" crate.

%files          devel
%{cargo_registry}/%{crate}-%{version_no_tilde}/

%package     -n %{name}+default-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+default-devel %{_description}

This package contains library source intended for building other packages
which use "default" feature of "%{crate}" crate.

%files       -n %{name}+default-devel
%ghost %{cargo_registry}/%{crate}-%{version_no_tilde}/Cargo.toml

%prep
%autosetup -n %{crate}-%{version_no_tilde} -p1
# Use latest rust-paste crate version 1.0.0
# https://github.com/little-dude/netlink/pull/98
sed -i -E '{N; '\
's/\[dependencies.paste\]\nversion = "0.1.6"/'\
'[dependencies.paste]\nversion = "1.0"/; '\
'P; D;}' Cargo.toml

%cargo_prep

%generate_buildrequires
%cargo_generate_buildrequires

%build
%cargo_build

%install
%cargo_install

%if %{with check}
%check
%cargo_test
%endif

%changelog
* Fri Oct 09 2020 Gris Ge <fge@redhat.com> - 0.3.0-1
- Upgrade to 0.3.0

* Mon Sep 07 2020 Gris Ge <fge@redhat.com> - 0.2.0-2
- Increate the depdent rust-paste to 1.0

* Tue Jul 07 16:43:54 CST 2020 Gris Ge <cnfourt@gmail.com> - 0.2.0-1
- Initial package
