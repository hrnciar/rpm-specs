# Generated by go2rpm 1
%bcond_without check

# https://github.com/mozillazg/go-pinyin
%global goipath         github.com/mozillazg/go-pinyin
Version:                0.18.0

%gometa

%global common_description %{expand:
This package provides tools and Golang library to convert Chinese characters
to Pinyin.}

%global golicenses      LICENSE
%global godocs          README.md CHANGELOG.md

Name:           %{goname}
Release:        2%{?dist}
Summary:        Tools and Golang library to convert Chinese characters to Pinyin

License:        MIT
URL:            %{gourl}
Source0:        %{gosource}

BuildRequires:  golang(github.com/mattn/go-isatty)

%description
%{common_description}

%gopkg

%prep
%goprep

%build
for cmd in cmd/* ; do
  %gobuild -o %{gobuilddir}/bin/$(basename $cmd) %{goipath}/$cmd
done
for cmd in _tools; do
  %gobuild -o %{gobuilddir}/bin/$(basename $cmd) %{goipath}/$cmd
done

%install
%gopkginstall
install -m 0755 -vd                     %{buildroot}%{_bindir}
install -m 0755 -vp %{gobuilddir}/bin/* %{buildroot}%{_bindir}/

%if %{with check}
%check
%gocheck
%endif

%files
%license LICENSE
%doc README.md CHANGELOG.md
%{_bindir}/*


%gopkgfiles

%changelog
* Thu Sep 17 2020 Robin Lee <cheeselee@fedoraproject.org> - 0.18.0-2
- Don't include pinyin-data
- Remove duplicated doc files

* Thu Aug  6 2020 Robin Lee <cheeselee@fedoraproject.org> - 0.18.0-1
- Update to 0.18.0

* Sun May 03 11:07:33 CST 2020 Robin Lee <cheeselee@fedoraproject.org> - 0.17.0-1
- Initial package

