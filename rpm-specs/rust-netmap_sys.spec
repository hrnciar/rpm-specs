# Generated by rust2rpm
%bcond_with check
%global debug_package %{nil}

%global crate netmap_sys

Name:           rust-%{crate}
Version:        0.1.4
Release:        3%{?dist}
Summary:        Bindings to netmap - the fast packet I/O framework

# Upstream license specification: BSD-2-Clause
# https://github.com/libpnet/netmap_sys/issues/21
License:        BSD
URL:            https://crates.io/crates/netmap_sys
Source:         %{crates_source}

ExclusiveArch:  %{rust_arches}

BuildRequires:  rust-packaging
BuildRequires:  (crate(cc/default) >= 1.0.35 with crate(cc/default) < 2.0.0)
BuildRequires:  (crate(libc/default) >= 0.2.0 with crate(libc/default) < 0.3.0)

%global _description \
Bindings to netmap - the fast packet I/O framework.

%description %{_description}

%package        devel
Summary:        %{summary}
BuildArch:      noarch

%description    devel %{_description}

This package contains library source intended for building other packages
which use "%{crate}" crate.

%files          devel
%doc README.md
%{cargo_registry}/%{crate}-%{version}/

%package     -n %{name}+default-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+default-devel %{_description}

This package contains library source intended for building other packages
which use "default" feature of "%{crate}" crate.

%files       -n %{name}+default-devel
%ghost %{cargo_registry}/%{crate}-%{version}/Cargo.toml

%package     -n %{name}+netmap_with_libs-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+netmap_with_libs-devel %{_description}

This package contains library source intended for building other packages
which use "netmap_with_libs" feature of "%{crate}" crate.

%files       -n %{name}+netmap_with_libs-devel
%ghost %{cargo_registry}/%{crate}-%{version}/Cargo.toml

%prep
%autosetup -n %{crate}-%{version_no_tilde} -p1
%cargo_prep

%build
%cargo_build

%install
%cargo_install

%if %{with check}
%check
%cargo_test
%endif

%changelog
* Thu Jan 30 2020 Fedora Release Engineering <releng@fedoraproject.org> - 0.1.4-3
- Rebuilt for https://fedoraproject.org/wiki/Fedora_32_Mass_Rebuild

* Fri Jul 26 2019 Fedora Release Engineering <releng@fedoraproject.org> - 0.1.4-2
- Rebuilt for https://fedoraproject.org/wiki/Fedora_31_Mass_Rebuild

* Wed Apr 17 2019 Josh Stone <jistone@redhat.com> - 0.1.4-1
- Update to 0.1.4

* Tue Apr 16 09:05:22 CEST 2019 Igor Gnatenko <ignatenkobrain@fedoraproject.org> - 0.1.3-2
- Fixes in packaging

* Wed Apr 10 2019 Sayan Chowdhury <sayanchowdhury@fedoraproject.org> - 0.1.3-1
- Initial package
