# Generated by rust2rpm 13
%bcond_with check
%global debug_package %{nil}

%global crate simple_asn1

Name:           rust-%{crate}
Version:        0.4.1
Release:        4%{?dist}
Summary:        Simple DER/ASN.1 encoding/decoding library

# Upstream license specification: ISC
License:        ISC
URL:            https://crates.io/crates/simple_asn1
Source:         %{crates_source}
# Increment num-bigint to 0.3.0, quickcheck to 0.9.2
Patch0:         simple_asn1-fix-metadata.diff

ExclusiveArch:  %{rust_arches}
%if %{__cargo_skip_build}
BuildArch:      noarch
%endif

BuildRequires:  rust-packaging

%global _description %{expand:
Simple DER/ASN.1 encoding/decoding library.}

%description %{_description}

%package        devel
Summary:        %{summary}
BuildArch:      noarch

%description    devel %{_description}

This package contains library source intended for building other packages
which use "%{crate}" crate.

%files          devel
%license LICENSE
%doc README.md
%{cargo_registry}/%{crate}-%{version_no_tilde}/

%package     -n %{name}+default-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+default-devel %{_description}

This package contains library source intended for building other packages
which use "default" feature of "%{crate}" crate.

%files       -n %{name}+default-devel
%ghost %{cargo_registry}/%{crate}-%{version_no_tilde}/Cargo.toml

%prep
%autosetup -n %{crate}-%{version_no_tilde} -p1
%cargo_prep

%generate_buildrequires
%cargo_generate_buildrequires

%build
%cargo_build

%install
%cargo_install

%if %{with check}
%check
%cargo_test
%endif

%changelog
* Sat Aug 01 2020 Fedora Release Engineering <releng@fedoraproject.org> - 0.4.1-4
- Second attempt - Rebuilt for
  https://fedoraproject.org/wiki/Fedora_33_Mass_Rebuild

* Wed Jul 29 2020 Fedora Release Engineering <releng@fedoraproject.org> - 0.4.1-3
- Rebuilt for https://fedoraproject.org/wiki/Fedora_33_Mass_Rebuild

* Tue Jul 21 2020 Peter Robinson <pbrobinson@fedoraproject.org> - 0.4.1-2
- Updates for review

* Sat Jul 18 2020 Peter Robinson <pbrobinson@fedoraproject.org> - 0.4.1-1
- Initial package
