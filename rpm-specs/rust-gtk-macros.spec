# Generated by rust2rpm 13
# * The doctests are simply broken and there are no functional ones
#   https://gitlab.gnome.org/haecker-felix/gtk-macros/-/issues/2
%bcond_with check
%global debug_package %{nil}

%global crate gtk-macros

Name:           rust-%{crate}
Version:        0.2.0
Release:        1%{?dist}
Summary:        Few macros to make gtk-rs development more convenient

# Upstream license specification: GPL-3.0-or-later
# https://gitlab.gnome.org/haecker-felix/gtk-macros/-/issues/1
License:        GPLv3+
URL:            https://crates.io/crates/gtk-macros
Source:         %{crates_source}

ExclusiveArch:  %{rust_arches}
%if %{__cargo_skip_build}
BuildArch:      noarch
%endif

BuildRequires:  rust-packaging

%global _description %{expand:
Few macros to make gtk-rs development more convenient.}

%description %{_description}

%package        devel
Summary:        %{summary}
BuildArch:      noarch

%description    devel %{_description}

This package contains library source intended for building other packages
which use "%{crate}" crate.

%files          devel
%doc README.md
%{cargo_registry}/%{crate}-%{version_no_tilde}/

%package     -n %{name}+default-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+default-devel %{_description}

This package contains library source intended for building other packages
which use "default" feature of "%{crate}" crate.

%files       -n %{name}+default-devel
%ghost %{cargo_registry}/%{crate}-%{version_no_tilde}/Cargo.toml

%prep
%autosetup -n %{crate}-%{version_no_tilde} -p1
%cargo_prep

%generate_buildrequires
%cargo_generate_buildrequires

%build
%cargo_build

%install
%cargo_install

%if %{with check}
%check
%cargo_test
%endif

%changelog
* Thu May 21 17:26:52 CEST 2020 Igor Raits <i.gnatenko.brain@gmail.com> - 0.2.0-1
- Initial package
