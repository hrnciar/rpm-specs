# Generated by rust2rpm 13
%bcond_without check
%global debug_package %{nil}

%global crate listenfd

Name:           rust-%{crate}
Version:        0.3.3
Release:        1%{?dist}
Summary:        Simple library to work with listenfds passed from the outside

# Upstream license specification: MIT/Apache-2.0
# https://github.com/mitsuhiko/rust-listenfd/issues/6
License:        MIT or ASL 2.0
URL:            https://crates.io/crates/listenfd
Source:         %{crates_source}
# Initial patched metadata
# * No windows
Patch0:         listenfd-fix-metadata.diff

ExclusiveArch:  %{rust_arches}
%if %{__cargo_skip_build}
BuildArch:      noarch
%endif

BuildRequires:  rust-packaging

%global _description %{expand:
Simple library to work with listenfds passed from the outside (systemd/catflap
socket activation).}

%description %{_description}

%package        devel
Summary:        %{summary}
BuildArch:      noarch

%description    devel %{_description}

This package contains library source intended for building other packages
which use "%{crate}" crate.

%files          devel
%doc README.md
%{cargo_registry}/%{crate}-%{version_no_tilde}/

%package     -n %{name}+default-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+default-devel %{_description}

This package contains library source intended for building other packages
which use "default" feature of "%{crate}" crate.

%files       -n %{name}+default-devel
%ghost %{cargo_registry}/%{crate}-%{version_no_tilde}/Cargo.toml

%prep
%autosetup -n %{crate}-%{version_no_tilde} -p1
%cargo_prep

%generate_buildrequires
%cargo_generate_buildrequires

%build
%cargo_build

%install
%cargo_install

%if %{with check}
%check
%cargo_test
%endif

%changelog
* Wed Mar 18 17:39:34 CET 2020 Igor Raits <ignatenkobrain@fedoraproject.org> - 0.3.3-1
- Initial package
