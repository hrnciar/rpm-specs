# Generated by rust2rpm 13
%bcond_without check

%global crate peg

Name:           rust-%{crate}0.5
Version:        0.5.7
Release:        1%{?dist}
Summary:        Simple Parsing Expression Grammar (PEG) parser generator

# Upstream license specification: MIT
License:        MIT
URL:            https://crates.io/crates/peg
Source:         %{crates_source}

ExclusiveArch:  %{rust_arches}
%if %{__cargo_skip_build}
BuildArch:      noarch
%endif

BuildRequires:  rust-packaging

%global _description %{expand:
Simple Parsing Expression Grammar (PEG) parser generator.}

%description %{_description}

%if ! %{__cargo_skip_build}
%package     -n %{crate}0.5
Summary:        %{summary}

%description -n %{crate}0.5 %{_description}

%files       -n %{crate}0.5
%license LICENSE
%doc README.md
%{_bindir}/rust-peg-0.5
%endif

%package        devel
Summary:        %{summary}
BuildArch:      noarch

%description    devel %{_description}

This package contains library source intended for building other packages
which use "%{crate}" crate.

%files          devel
%license LICENSE
%doc README.md
%{cargo_registry}/%{crate}-%{version_no_tilde}/
%exclude %{cargo_registry}/%{crate}-%{version}/bootstrap.sh

%package     -n %{name}+default-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+default-devel %{_description}

This package contains library source intended for building other packages
which use "default" feature of "%{crate}" crate.

%files       -n %{name}+default-devel
%ghost %{cargo_registry}/%{crate}-%{version_no_tilde}/Cargo.toml

%package     -n %{name}+trace-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+trace-devel %{_description}

This package contains library source intended for building other packages
which use "trace" feature of "%{crate}" crate.

%files       -n %{name}+trace-devel
%ghost %{cargo_registry}/%{crate}-%{version_no_tilde}/Cargo.toml

%prep
%autosetup -n %{crate}-%{version_no_tilde} -p1
%cargo_prep

%generate_buildrequires
%cargo_generate_buildrequires

%build
%cargo_build

%install
%cargo_install

# rename binary so it doesn't conflict with peg 0.6
mv %{buildroot}/%{_bindir}/rust-peg %{buildroot}/%{_bindir}/rust-peg-0.5

%if %{with check}
%check
%cargo_test
%endif

%changelog
* Tue Oct 06 2020 Fabio Valentini <decathorpe@gmail.com> - 0.5.7-1
- Initial compat package for peg 0.5
