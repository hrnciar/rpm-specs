# Generated by rust2rpm 13
%bcond_without check
%global debug_package %{nil}

%global crate migrations_internals

Name:           rust-%{crate}
Version:        1.4.1
Release:        1%{?dist}
Summary:        Internal implementation of diesels migration mechanism

# Upstream license specification: MIT OR Apache-2.0
# https://github.com/diesel-rs/diesel/pull/2395
License:        MIT or ASL 2.0
URL:            https://crates.io/crates/migrations_internals
Source:         %{crates_source}

ExclusiveArch:  %{rust_arches}
%if %{__cargo_skip_build}
BuildArch:      noarch
%endif

BuildRequires:  rust-packaging

%global _description %{expand:
Internal implementation of diesels migration mechanism.}

%description %{_description}

%package        devel
Summary:        %{summary}
BuildArch:      noarch

%description    devel %{_description}

This package contains library source intended for building other packages
which use "%{crate}" crate.

%files          devel
%{cargo_registry}/%{crate}-%{version_no_tilde}/

%package     -n %{name}+default-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+default-devel %{_description}

This package contains library source intended for building other packages
which use "default" feature of "%{crate}" crate.

%files       -n %{name}+default-devel
%ghost %{cargo_registry}/%{crate}-%{version_no_tilde}/Cargo.toml

%package     -n %{name}+barrel-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+barrel-devel %{_description}

This package contains library source intended for building other packages
which use "barrel" feature of "%{crate}" crate.

%files       -n %{name}+barrel-devel
%ghost %{cargo_registry}/%{crate}-%{version_no_tilde}/Cargo.toml

%prep
%autosetup -n %{crate}-%{version_no_tilde} -p1
%cargo_prep

%generate_buildrequires
%cargo_generate_buildrequires

%build
%cargo_build

%install
%cargo_install

%if %{with check}
%check
%cargo_test
%endif

%changelog
* Fri May 15 08:04:33 CEST 2020 Igor Raits <i.gnatenko.brain@gmail.com> - 1.4.1-1
- Initial package
