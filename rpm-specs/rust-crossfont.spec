# Generated by rust2rpm 13
%bcond_without check
%global debug_package %{nil}

%global crate crossfont

Name:           rust-%{crate}
Version:        0.1.0
Release:        1%{?dist}
Summary:        Cross platform native font loading and rasterization

# Upstream license specification: Apache-2.0
License:        ASL 2.0
URL:            https://crates.io/crates/crossfont
Source:         %{crates_source}
# Initial patched metadata
# Removes other platforms and bumps euclid to version 0.22
# https://github.com/alacritty/crossfont/pull/10
Patch0:         crossfont-fix-metadata.diff

ExclusiveArch:  %{rust_arches}
%if %{__cargo_skip_build}
BuildArch:      noarch
%endif

BuildRequires:  rust-packaging

%global _description %{expand:
Cross platform native font loading and rasterization.}

%description %{_description}

%package        devel
Summary:        %{summary}
BuildArch:      noarch

%description    devel %{_description}

This package contains library source intended for building other packages
which use "%{crate}" crate.

%files          devel
# License file not yet in crate:
# https://github.com/alacritty/crossfont/commit/1a957aff834d54ee0847a3bde2e59621a3d8f2d1
%doc README.md
%{cargo_registry}/%{crate}-%{version_no_tilde}/

%package     -n %{name}+default-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+default-devel %{_description}

This package contains library source intended for building other packages
which use "default" feature of "%{crate}" crate.

%files       -n %{name}+default-devel
%ghost %{cargo_registry}/%{crate}-%{version_no_tilde}/Cargo.toml

%package     -n %{name}+force_system_fontconfig-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+force_system_fontconfig-devel %{_description}

This package contains library source intended for building other packages
which use "force_system_fontconfig" feature of "%{crate}" crate.

%files       -n %{name}+force_system_fontconfig-devel
%ghost %{cargo_registry}/%{crate}-%{version_no_tilde}/Cargo.toml

%prep
%autosetup -n %{crate}-%{version_no_tilde} -p1
%cargo_prep

%generate_buildrequires
%cargo_generate_buildrequires

%build
%cargo_build

%install
%cargo_install

%if %{with check}
%check
%cargo_test
%endif

%changelog
* Thu Oct 15 23:16:09 CEST 2020 returntrip <stefano@figura.im> - 0.1.0-1
- Initial package
